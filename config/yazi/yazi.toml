# Keep this file intentionally minimal so Yazi inherits current built-in defaults.
# A TOML linter such as https://github.com/tombi-toml/tombi can use this schema.
#:schema https://yazi-rs.github.io/schemas/yazi.json

[mgr]
ratio = [ 2, 3, 4 ]
show_hidden = true
