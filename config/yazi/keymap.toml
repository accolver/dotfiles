# Keep this file intentionally minimal so Yazi inherits the current built-in keymap.
# Add custom bindings with prepend_keymap or append_keymap instead of copying the full preset.
#:schema https://yazi-rs.github.io/schemas/keymap.json
