# Local secrets file (contains API keys and tokens)
.zshrc.local

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor files
*.swp
*.swo
*~

# Backup files
*.bak
*.bak.*

# State files that shouldn't be synced
config/ncspot/userstate.cbor
config/yazi/*.toml-*

# Node modules (if any config has them)
node_modules/
bun.lock
